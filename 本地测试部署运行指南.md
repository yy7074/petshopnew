# 🐾 拍宠有道 - 本地测试部署运行指南

## 📋 项目概览

**拍宠有道**是一个功能完整的宠物拍卖平台，采用前后端分离架构：

- **前端**: Flutter 移动端应用 (iOS/Android/Web)
- **后端**: Python FastAPI + MySQL 数据库
- **管理端**: HTML + JavaScript 后台管理系统
- **域名**: https://catdog.dachaonet.com (内网穿透)

## 🏗️ 技术架构

### 核心技术栈
- **移动端**: Flutter 3.29.2 + Dart 3.7.2
- **后端**: FastAPI 0.104.1 + SQLAlchemy ORM
- **数据库**: MySQL 9.2.0 + Redis 6.2.6
- **认证**: JWT Token
- **支付**: 支付宝SDK (正式环境)
- **通信**: WebSocket 实时消息

### 项目结构
```
petshopnew/
├── petshop_app/              # Flutter移动端应用
│   ├── lib/                  # Dart源码
│   ├── android/              # Android配置
│   ├── ios/                  # iOS配置
│   └── pubspec.yaml          # Flutter依赖配置
├── backend/                  # Python后端API
│   ├── app/                  # 应用源码
│   ├── static/uploads/       # 文件上传目录
│   ├── requirements.txt      # Python依赖
│   ├── main.py              # 启动入口
│   └── .env.local           # 环境配置
├── admin/                    # HTML后台管理
│   ├── index.html           # 管理界面
│   └── js/admin.js          # 管理脚本
├── database_design.sql       # MySQL数据库设计
└── 本地测试部署运行指南.md     # 本文档
```

## 🔧 环境要求

### 必需环境
- **Python**: 3.10+ ✅
- **Flutter**: 3.29.2+ ✅
- **MySQL**: 9.0+ ✅
- **Redis**: 6.0+ ✅
- **Node.js**: 16+ (可选)

### 开发工具推荐
- **IDE**: VS Code / Android Studio / PyCharm
- **API测试**: Postman / Swagger UI
- **数据库管理**: MySQL Workbench / phpMyAdmin
- **版本控制**: Git

## 🚀 快速部署指南

### 1. 环境准备

#### 检查环境版本
```bash
python --version    # Python 3.10.17
flutter --version   # Flutter 3.29.2
mysql --version     # MySQL 9.2.0
redis-cli --version # Redis 6.2.6
```

#### 启动基础服务
```bash
# 启动MySQL服务
brew services start mysql
# 或者
sudo systemctl start mysql

# 启动Redis服务
brew services start redis
# 或者
sudo systemctl start redis
```

### 2. 数据库初始化

#### 创建数据库
```bash
# 连接MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE petshop_auction CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 导入数据结构（可选）
mysql -u root -p petshop_auction < database_design.sql
```

#### 验证数据库
```bash
# 检查表结构
mysql -u root -p -e "USE petshop_auction; SHOW TABLES;"
```

### 3. 后端服务部署

#### 安装Python依赖
```bash
cd backend
pip install -r requirements.txt
```

#### 配置环境变量
检查 `.env.local` 文件配置：
```bash
# 数据库配置
DATABASE_URL=mysql+pymysql://root:123456@localhost:3306/petshop_auction

# Redis配置
REDIS_URL=redis://localhost:6379/0

# JWT配置
SECRET_KEY=your-secret-key-here-change-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=43200

# 支付宝配置（已配置正式环境）
ALIPAY_APP_ID=2021003142685803
ALIPAY_PRIVATE_KEY=已配置
ALIPAY_PUBLIC_KEY=已配置

# 短信配置（阿里云）
SMS_ACCESS_KEY=LTAI5tCYSifRd85fqmc6PvV8
SMS_SECRET_KEY=已配置
SMS_SIGN_NAME=大潮网络
SMS_TEMPLATE_ID=SMS_474780238
```

#### 启动后端服务
```bash
cd backend

# 方式1: 直接运行
python main.py

# 方式2: 使用uvicorn
uvicorn main:app --reload --host 0.0.0.0 --port 3000

# 服务启动成功标志
# INFO: Uvicorn running on http://0.0.0.0:3000 (Press CTRL+C to quit)
```

#### 验证后端服务
```bash
# 健康检查
curl http://localhost:3000/health

# API文档
curl http://localhost:3000/docs

# 测试接口
curl http://localhost:3000/api/v1/products/categories/
```

### 4. 内网穿透配置

项目已配置域名：`catdog.dachaonet.com`

#### 验证外网访问
```bash
# 健康检查
curl https://catdog.dachaonet.com/health

# API接口
curl https://catdog.dachaonet.com/api/v1/products/categories/

# 响应示例：
# [{"id":1,"name":"宠物","parent_id":0,...}]
```

### 5. Flutter应用部署

#### 安装Flutter依赖
```bash
cd petshop_app
flutter pub get
```

#### 检查设备
```bash
flutter devices

# 示例输出：
# Pixel 6 (mobile)                • 25201FDF6007VR        • android-arm64
# macOS (desktop)                 • macos                 • darwin-arm64
# Chrome (web)                    • chrome                • web-javascript
```

#### 验证API配置
Flutter应用已配置使用线上域名：
```dart
// lib/constants/api_constants.dart
static String get baseUrl {
  return 'https://catdog.dachaonet.com/api/v1';
}
```

#### 启动Flutter应用
```bash
# Android设备
flutter run -d android

# iOS设备  
flutter run -d ios

# Web浏览器
flutter run -d chrome

# macOS桌面
flutter run -d macos

# 指定设备
flutter run -d 25201FDF6007VR
```

## 🧪 功能测试

### API接口测试

#### 基础接口
```bash
# 根路径
curl https://catdog.dachaonet.com/
# 响应: {"message":"宠物拍卖API服务","version":"1.0.0"}

# 健康检查
curl https://catdog.dachaonet.com/health
# 响应: {"status":"healthy","message":"服务运行正常"}

# 商品分类
curl https://catdog.dachaonet.com/api/v1/products/categories/
# 响应: [{"id":1,"name":"宠物",...}]
```

#### 使用测试脚本
```bash
cd backend

# 综合API测试
python test_api.py

# 同城服务测试
python test_local_services_api.py

# AI识别测试
python test_recognition_api.py
```

### 数据初始化

#### 创建演示数据
```bash
cd backend

# 初始化基础演示数据
python init_demo_data.py

# 初始化抽奖数据
python init_lottery_data.py

# 初始化店铺数据
python init_store_demo_data.py

# 初始化同城服务数据
python init_local_service_demo_data.py
```

### Flutter应用测试

#### 功能模块测试
1. **用户注册/登录**
2. **商品浏览/搜索**
3. **实时拍卖**
4. **订单管理**
5. **消息聊天**
6. **同城服务**
7. **签到抽奖**
8. **支付功能**

## 🔍 故障排查

### 常见问题解决

#### 1. 后端服务启动失败
```bash
# 检查端口占用
lsof -i :3000

# 检查MySQL连接
mysql -u root -p123456 -e "SELECT 1"

# 检查Redis连接
redis-cli ping
```

#### 2. 数据库连接失败
```bash
# 检查MySQL服务状态
brew services list | grep mysql

# 重启MySQL服务
brew services restart mysql

# 检查数据库配置
cat backend/.env.local | grep DATABASE_URL
```

#### 3. Flutter构建失败
```bash
# 清理缓存
flutter clean
flutter pub get

# 检查Flutter环境
flutter doctor

# 检查Android SDK
flutter doctor --android-licenses
```

#### 4. 网络请求失败
```bash
# 检查API域名解析
nslookup catdog.dachaonet.com

# 检查网络连接
curl -I https://catdog.dachaonet.com/health

# 检查Flutter网络配置
# android/app/src/main/AndroidManifest.xml
# 确保有网络权限
```

### 日志查看

#### 后端服务日志
```bash
cd backend

# 实时查看日志
tail -f server.log

# 查看错误日志
grep ERROR server.log
```

#### Flutter应用日志
```bash
# 运行时查看日志
flutter logs

# 设备日志
adb logcat | grep flutter
```

## 📱 访问地址

### 后端服务
- **API根地址**: https://catdog.dachaonet.com/
- **API文档**: https://catdog.dachaonet.com/docs
- **健康检查**: https://catdog.dachaonet.com/health
- **后台管理**: https://catdog.dachaonet.com/admin

### 本地服务
- **本地API**: http://localhost:3000
- **本地文档**: http://localhost:3000/docs
- **本地管理**: http://localhost:3000/admin

## 🎯 核心功能

### 已实现功能 ✅
- ✅ 用户注册登录 (手机号+短信验证)
- ✅ 商品管理 (发布/编辑/分类)
- ✅ 实时拍卖 (WebSocket通信)
- ✅ 订单系统 (下单/支付/物流)
- ✅ 消息聊天 (私信/系统通知)
- ✅ 同城服务 (本地服务发布)
- ✅ 签到抽奖 (每日签到+抽奖)
- ✅ 关注系统 (用户关注/粉丝)
- ✅ 支付集成 (支付宝正式环境)
- ✅ AI识别 (宠物品种识别)
- ✅ 店铺申请 (商家入驻)
- ✅ 后台管理 (数据统计/管理)

### 数据库表结构
- **用户相关**: users, user_follows, user_addresses, user_checkins
- **商品相关**: products, categories, bids, product_favorites
- **交易相关**: orders, order_items, payments, logistics
- **内容相关**: messages, conversations, local_services
- **系统相关**: special_events, ai_recognition_records

## 🛡️ 安全配置

### 生产环境安全要点
1. **JWT密钥**: 使用强随机密钥
2. **数据库密码**: 使用复杂密码
3. **API限流**: 防止恶意请求
4. **HTTPS**: 全站HTTPS加密
5. **文件上传**: 限制文件类型和大小
6. **SQL注入**: 使用ORM防护
7. **XSS防护**: 前端输入验证

### 当前配置状态
- ✅ JWT Token认证
- ✅ MySQL数据库加密
- ✅ HTTPS域名访问
- ✅ 文件上传限制
- ✅ CORS跨域配置
- ✅ 支付宝正式环境

## 📊 性能监控

### 关键指标
- **响应时间**: API平均响应 < 100ms
- **并发处理**: 支持1000+并发用户
- **数据库连接**: 连接池优化
- **缓存策略**: Redis缓存热点数据
- **文件存储**: 静态文件CDN加速

## 🔄 版本发布

### 发布流程
1. **代码测试**: 运行完整测试套件
2. **构建应用**: Flutter构建发布版本
3. **数据备份**: 备份生产数据库
4. **服务部署**: 更新后端服务
5. **应用发布**: 发布移动应用

### 版本信息
- **当前版本**: v1.0.0
- **API版本**: v1
- **数据库版本**: MySQL 9.2.0
- **Flutter版本**: 3.29.2

## 📞 联系支持

### 技术支持
- **项目维护**: 开发团队
- **技术文档**: 本文档
- **问题反馈**: GitHub Issues
- **更新日志**: NEW_FEATURES.md

### 相关文档
- **数据库设计**: database_design.sql
- **API文档**: /docs (Swagger UI)
- **配置说明**: README_CONFIG.md
- **支付集成**: payment_setup_guide.md

---

## 🎉 快速启动命令总结

```bash
# 1. 启动后端服务
cd backend && python main.py

# 2. 新终端窗口 - 启动Flutter应用
cd petshop_app && flutter run

# 3. 验证服务
curl https://catdog.dachaonet.com/health

# 4. 访问API文档
open https://catdog.dachaonet.com/docs

# 5. 访问后台管理
open https://catdog.dachaonet.com/admin
```

**🎯 至此，拍宠有道平台本地测试环境部署完成！**
